pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                sh 'javac MultiplicationOfTwoNumbers.java'
            }
        }
    }
    post { 
        always { 
            echo 'I will always say Hello again!'
        }
    }
}
