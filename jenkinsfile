pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                sh 'javac AddTwoNumbers.java'
            }
        }
    }
    post { 
        always { 
            echo 'I will always say Hello again!'
        }
    }
}
